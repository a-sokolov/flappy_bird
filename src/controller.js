export class Controller {
  constructor(game) {
    this.game = game
  }

  init() {
    // init controller
  }

  destroy() {
    // destroy controller
  }

  render(time) {
    // render controller
  }
}